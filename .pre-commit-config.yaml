repos:
  - repo: local
    hooks:
      - id: release-gate
        name: maestro-memory release gate
        entry: ./scripts/check.sh
        language: system
        pass_filenames: false
        always_run: true
